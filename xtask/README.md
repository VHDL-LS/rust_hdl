# xtask

[xtask](https://github.com/matklad/cargo-xtask) is a common pattern to add automation to a rust project.

This tool generated code for `vhdl_syntax`'s AST node definitions.

## Re-generating nodes

Run

```sh
cargo xtask codegen
```

to regenerate design files in vhdl_syntax.
