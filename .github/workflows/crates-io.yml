name: Crates.io

on:
  push:
    tags:
      # Only the workspace release tags. release-plz tags its own crates as
      # `<crate>-v<version>`, which must not trigger this workflow.
      - 'v*'

jobs:
  publish:
    name: Publish
    if: github.repository == 'VHDL-LS/rust_hdl'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
      - uses: actions-rs/toolchain@b2417cde72dcf67f306c0ae8e0828a81bf0b189f  # v1.0.6
        with:
          toolchain: stable
          override: true
      - uses: katyo/publish-crates@02cc2f1ad653fb25c7d1ff9eb590a8a50d06186b  # v2
        with:
          registry-token: ${{ secrets.CRATES_IO_TOKEN }}
          ignore-unpublished-changes: true